"use strict"; // treat all JS code as newer version

// alert("hello") // here we are using nodejs , not browser

// try to write code such that readibility should be high

let name="Mukul" // string type defined by '' or ""
let age=20 // integer or number type
let isLoggedIn=false // boolean type either trur or false
let state=null// null=> standalone value
let k// undefined => basicall variable is declared but not defined or assigned to it
// symbol => used for uniqueness



// Object
// typeof is used to know the datatype of variables
console.log(typeof "mukul"); //string tyep
console.log(typeof(age)); // number type
console.log(typeof(state)); // object type
console.log(typeof(k)); //undefined type